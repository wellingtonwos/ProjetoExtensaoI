package com.example.SpringBootApp.types;

/**
 * Stubbed PostgreSQLEnumType.
 * The previous custom Hibernate UserType caused compilation problems across Hibernate versions.
 * This stub keeps the class present (no-op) while the project uses the JDBC driver setting
 * `stringtype=unspecified` to allow enum parameters to be accepted by Postgres.
 */
public final class PostgreSQLEnumType {
    // Intentionally left blank - original implementation disabled.
}
